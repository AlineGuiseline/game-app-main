import { LoadingContainer, Loading } from "./styles";

const LoadingSpinner = () => {
    return (
        <LoadingContainer>
            <Loading/>
        </LoadingContainer>
    )
}

export default LoadingSpinner;